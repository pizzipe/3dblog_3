version: '3.8'

services:
  directus:
    image: directus/directus:latest
    ports:
      - 8055:8055
    volumes:
      - ./database:/directus/database
    environment:
      KEY: supersecretkey
      SECRET: anothersecret
      ADMIN_EMAIL: begonzo82@gmail.com
      ADMIN_PASSWORD: admin123
      CORS_ENABLED: true
      CORS_ORIGIN: http://localhost:4200
